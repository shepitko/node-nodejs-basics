export const handleFileBaseActions = ({ cmd, args, output }) => {
	if (cmd === CAT) {
	}

	if (cmd === ADD) {
	}

	if (cmd === RN) {
	}

	if (cmd === CP) {
	}

	if (cmd === MV) {
	}

	if (cmd === RM) {
	}
}
